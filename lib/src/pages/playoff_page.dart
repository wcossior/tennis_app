import 'package:flutter/material.dart';

class PlayoffPage extends StatelessWidget {
  const PlayoffPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Eliminacion pagina")),
    );
  }
}